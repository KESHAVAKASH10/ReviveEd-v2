export default function LoginForm({
    role,
    name,
    setName,
    password,
    setPassword,
    error,
    loading,
    passwordFocused,
    setPasswordFocused,
    handleLogin,
    activeRole,
}) {
    return (
        <>
            {/* move input fields + button + error here */}
        </>
    )
}